info:
  name: alterStatusNonExistent
  type: http
  seq: 8

http:
  method: PATCH
  url: localhost:8080/schedules/999/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
